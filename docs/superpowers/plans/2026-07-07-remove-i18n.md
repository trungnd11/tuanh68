# Remove i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remove all i18n infrastructure, locale routing, and translation hooks from the web app — single-language Vietnamese only.

**Architecture:** Delete the i18n core files, flatten `[locale]` directory to root, replace every `useTranslations`/`getTranslations` call with static Vietnamese text, and remove the language switcher UI.

**Tech Stack:** Next.js 16, next-intl (being removed)

## Global Constraints

- No `next-intl` import may remain after this plan is complete
- All locale-based URL paths (`/vi/...`, `/en/...`) become root paths (`/...`)
- Investor document metadata (VN + ENG files) stays intact; only locale filtering logic is removed
- SEO metadata stays hardcoded to `vi_VN` (already the case)

---

### Task 1: Remove i18n infrastructure

**Files:**

- Delete: `apps/web/src/i18n/routing.ts`
- Delete: `apps/web/src/i18n/navigation.ts`
- Delete: `apps/web/src/i18n/request.ts`
- Delete: `apps/web/src/proxy.ts`
- Modify: `apps/web/next.config.ts`
- Modify: `apps/web/package.json`

- [ ] **Step 1: Delete i18n core files**

Delete `src/i18n/routing.ts`, `src/i18n/navigation.ts`, `src/i18n/request.ts`.

- [ ] **Step 2: Delete proxy middleware**

Delete `src/proxy.ts`.

- [ ] **Step 3: Update next.config.ts**

Remove `createNextIntlPlugin` import and wrapper:

```
  // Remove line 1: import createNextIntlPlugin from "next-intl/plugin";
  // Remove line 10: const withNextIntl = createNextIntlPlugin();
  // Remove line 78: wrap export: change `export default withNextIntl(nextConfig)` → `export default nextConfig`
```

- [ ] **Step 4: Update package.json**

Remove `"next-intl": "^4.13.0"` from dependencies.

- [ ] **Step 5: Commit**

Run: `git add -A && git commit -m "refactor: remove next-intl i18n infrastructure"`

---

### Task 2: Flatten routing — merge [locale] into root

**Files:**

- Modify: `apps/web/src/app/layout.tsx`
- Modify: `apps/web/src/app/page.tsx`
- Delete: `apps/web/src/app/[locale]/layout.tsx`
- Delete: `apps/web/src/app/[locale]/page.tsx`
- Move: `apps/web/src/app/[locale]/documents/[key]/route.ts` → `apps/web/src/app/documents/[key]/route.ts`
- Delete: `apps/web/src/app/[locale]/` (empty after moves)

- [ ] **Step 1: Update root layout.tsx**

Merge the locale layout's providers and structure into root layout:

Root layout (`src/app/layout.tsx`):

- Remove `import { getLocale } from "next-intl/server"`
- Replace `const locale = await getLocale()` → hardcode `const locale = "vi"`
- Merge in `NextIntlClientProvider` replacement: add `ReactQueryProvider` directly
- Add `AppScrollReset`, `AppHeader`, `AppMain`, `AppFooter`, `AppScrollTop` from locale layout

The merged file should look like:

```tsx
import { SquircleNoScript } from "@squircle-js/react";
import { Inter } from "next/font/google";
import type { Metadata } from "next";
import type { ReactNode } from "react";
import AppFooter from "@/shared/ui/app-footer";
import AppHeader from "@/shared/ui/app-header";
import AppMain from "@/shared/ui/app-main";
import AppScrollReset from "@/shared/ui/app-scroll-reset";
import AppScrollTop from "@/shared/ui/app-scroll-top";
import { ReactQueryProvider } from "@/shared/providers/reactQueryProvider";
import "./globals.css";

const siteUrl = process.env.SITE_URL;

if (!siteUrl) {
  throw new Error("Missing SITE_URL");
}

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  title: {
    default: "F88 - Cổ phiếu",
    template: "%s | F88 - Cổ phiếu",
  },
  description:
    "Landing page giới thiệu cổ phiếu F88 với thông tin doanh nghiệp, lộ trình IPO, tài liệu nhà đầu tư và hướng dẫn đăng ký mua cổ phiếu.",
  icons: {
    icon: "/icon.svg",
    shortcut: "/icon.svg",
  },
  alternates: {
    canonical: "/",
  },
  openGraph: {
    locale: "vi_VN",
    siteName: "F88",
    type: "website",
  },
};

export default async function RootLayout({
  children,
}: Readonly<{
  children: ReactNode;
}>) {
  return (
    <html lang="vi" className="h-full antialiased">
      <body className={`${inter.className} bg-main-app-bg-content`} suppressHydrationWarning>
        <script
          dangerouslySetInnerHTML={{
            __html: `(function(){try{new MutationObserver(function(m){m.forEach(function(r){if(r.type==="attributes"&&(r.attributeName==="bis_skin_checked"||r.attributeName==="bis_register")){r.target.removeAttribute(r.attributeName)}})}).observe(document.documentElement,{subtree:true,attributes:true,attributeFilter:["bis_skin_checked","bis_register"]})}catch(e){}})()`,
          }}
        />
        <SquircleNoScript />
        <ReactQueryProvider>
          <AppScrollReset />
          <AppHeader />
          <AppMain>{children}</AppMain>
          <AppFooter />
          <AppScrollTop />
        </ReactQueryProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Replace page.tsx content**

Replace `src/app/page.tsx` (the redirect page) with content from `[locale]/page.tsx`:

New page.tsx:

```tsx
import BannerSection from "@/app/sections/banner";
import F88BreakthroughGrowthSection from "@/app/sections/f88-breakthrough-growth";
import F88JourneySection from "@/app/sections/f88-journey";
import F88MarketLeaderSection from "@/app/sections/f88-market-leader";
import F88OverviewSection from "@/app/sections/f88-overview";
import F88StrategicDirectionSection from "@/app/sections/f88-strategic-direction";
import InvestorFaqSection from "@/app/sections/investor-faq";
import InvestorMaterialsSection from "@/app/sections/investor-materials";
import IpoRoadmapSection from "@/app/sections/ipo-roadmap";
import NewsSection from "@/app/sections/news-section";
import OfferingInfoSection from "@/app/sections/offering-info";
import PurchaseGuideSection from "@/app/sections/purchase-guide";
import AppSectionNavigator from "@/shared/ui/app-section-navigator";
import AppScrollReveal from "@/shared/ui/app-scroll-reveal";

export default function HomePage() {
  return (
    <>
      <AppSectionNavigator />
      <AppScrollReveal variant="fade-in-up">
        <BannerSection />
        <OfferingInfoSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <IpoRoadmapSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <F88OverviewSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <F88JourneySection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <F88StrategicDirectionSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <F88MarketLeaderSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <F88BreakthroughGrowthSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <InvestorMaterialsSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <InvestorFaqSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <NewsSection />
      </AppScrollReveal>
      <AppScrollReveal variant="fade-in-up">
        <PurchaseGuideSection />
      </AppScrollReveal>
    </>
  );
}
```

- [ ] **Step 3: Move documents route**

Move `src/app/[locale]/documents/[key]/route.ts` → `src/app/documents/[key]/route.ts` (no content changes needed — the route handler doesn't use locale).

- [ ] **Step 4: Delete [locale] directory**

Delete `src/app/[locale]/` (all files have been moved).

- [ ] **Step 5: Commit**

Run: `git add -A && git commit -m "refactor: flatten [locale] routing to root"`

---

### Task 3: Update types and config — remove AppLocale

**Files:**

- Modify: `apps/web/src/shared/config/section-config.ts`
- Modify: `apps/web/src/shared/ui/app-section-navigator/types.ts`
- Modify: `apps/web/src/shared/ui/app-section-navigator/AppSectionNavigator.tsx`
- Read: `apps/web/src/shared/ui/app-section-navigator/index.ts`

- [ ] **Step 1: Update section-config.ts**

Remove `AppLocale` import, remove `en` translations, simplify:

```ts
export const appSectionIds = {
  banner: "banner",
  offeringInfo: "offering-info",
  ipoRoadmap: "ipo-roadmap",
  f88Overview: "f88-overview",
  f88Journey: "f88-journey",
  f88StrategicDirection: "f88-strategic-direction",
  f88MarketLeader: "f88-market-leader",
  f88BreakthroughGrowth: "f88-breakthrough-growth",
  investorMaterials: "investor-materials",
  investorFaq: "investor-faq",
  newsSection: "news-section",
  purchaseGuide: "purchase-guide",
} as const;

export type AppSectionKey = keyof typeof appSectionIds;
export type AppSectionId = (typeof appSectionIds)[AppSectionKey];

export const appSectionTitles: Record<AppSectionKey, string> = {
  banner: "F88 PO",
  offeringInfo: "Thông tin chào bán",
  f88Overview: "Tổng quan về F88",
  f88MarketLeader: "Vị thế dẫn đầu",
  f88BreakthroughGrowth: "Kết quả kinh doanh",
  f88StrategicDirection: "Định hướng chiến lược",
  f88Journey: "Hành trình F88",
  ipoRoadmap: "Lộ trình PO",
  investorMaterials: "Tài liệu nhà đầu tư",
  purchaseGuide: "Hướng dẫn mua",
  newsSection: "Tin tức",
  investorFaq: "Câu hỏi thường gặp",
} as const;
```

- [ ] **Step 2: Update AppSectionNavigator types.ts**

Remove `AppLocale` import and `locale` prop:

```ts
import { ReactNode } from "react";

export interface AppSectionNavigatorProps {
  headerOffset?: number;
  logo?: ReactNode;
  className?: string;
}
```

- [ ] **Step 3: Update AppSectionNavigator.tsx**

Read the file first, then:

- Remove `locale` prop from the component signature
- Remove any `AppLocale` references
- Update `appSectionTitles` usage from `appSectionTitles[locale][key]` to `appSectionTitles[key]`

- [ ] **Step 4: Commit**

Run: `git add -A && git commit -m "refactor: remove AppLocale type and simplify section config"`

---

### Task 4: Remove HeaderLanguageButton

**Files:**

- Modify: `apps/web/src/shared/ui/app-header/components/header-actions/index.tsx`
- Modify: `apps/web/src/shared/ui/app-header/components/header-mobile-menu/index.tsx`

- [ ] **Step 1: Update header-actions/index.tsx**

Remove the `HeaderLanguageButton` function. Remove imports:

- `import { useLocale, useTranslations } from "next-intl"`
- `import GlobalIcon from "@/assets/icons/global.svg"`
- `import { usePathname, useRouter } from "@/i18n/navigation"`
- `import { getNextLocale, type AppLocale } from "@/i18n/routing"`
- `import AppBorderRadius from "@/shared/ui/app-border-radius"` (if only used by language button)

Update the main `HeaderActions` component to no longer render `HeaderLanguageButton()`.

Keep `HeaderPurchaseButton` — it still needs `useTranslations` for now (will be removed in a later task).

- [ ] **Step 2: Update header-mobile-menu/index.tsx**

Remove `<HeaderLanguageButton mobile />` from the JSX. Remove the `HeaderLanguageButton` import. Keep other imports for now.

- [ ] **Step 3: Commit**

Run: `git add -A && git commit -m "feat: remove language switcher button from header"`

---

### Task 5: Replace translations in investor-materials (server-side)

**Files:**

- Modify: `apps/web/src/app/sections/investor-materials/index.tsx`
- Modify: `apps/web/src/app/sections/investor-materials/data.ts`
- Modify: `apps/web/src/app/sections/investor-materials/components/investor-materials-heading/index.tsx`
- Modify: `apps/web/src/app/sections/investor-materials/components/investor-materials-document-grid/index.tsx`

- [ ] **Step 1: Update data.ts**

Replace translation function calls with static Vietnamese text:

```ts
import type { InvestorServedDocument } from "@/app/sections/investor-materials/server/documents";

export type InvestorMaterialFilter = {
  key: string;
  label: string;
};

export type InvestorDocumentItem = {
  title: string;
  subtitle: string;
  href: string;
  downloadHref: string;
};

export type InvestorDocumentGroup = {
  key: string;
  items: InvestorDocumentItem[];
  lessLabel: string;
  moreHref: string;
};

export function getInvestorMaterialFilters(): InvestorMaterialFilter[] {
  return [
    { key: "legal", label: "Kết quả kinh doanh" },
    { key: "ir", label: "Tài liệu IPO" },
  ];
}

export function getInvestorMaterialsHeading() {
  return {
    title: "Tài liệu nhà đầu tư",
    description: "Cập nhật thông tin mới nhất về tình hình hoạt động và kết quả kinh doanh của F88",
    mobileBreakAfter: "",
  };
}

export function getInvestorMaterialsMoreLabel(groupKey: InvestorDocumentGroup["key"], count: number) {
  return groupKey === "legal" ? `Xem thêm (${count})` : `Xem thêm (${count})`;
}

function formatPublishedAt(publishedAt: string) {
  const [year, month, day] = publishedAt.split("-");
  return `Ngày đăng: ${day}/${month}/${year}`;
}

export function buildInvestorDocumentGroups(
  documents: InvestorServedDocument[]
): Record<string, InvestorDocumentGroup> {
  const localizedDocuments = documents.filter((item) => item.locale === "VN");

  return {
    legal: {
      key: "legal",
      lessLabel: "Thu gọn",
      moreHref: "#",
      items: localizedDocuments
        .filter((item) => item.category === "legal")
        .map((item) => ({
          title: item.title,
          subtitle: formatPublishedAt(item.publishedAt),
          href: item.href,
          downloadHref: item.downloadHref,
        })),
    },
    ir: {
      key: "ir",
      lessLabel: "Thu gọn",
      moreHref: "#",
      items: localizedDocuments
        .filter((item) => item.category === "ir")
        .map((item) => ({
          title: item.title,
          subtitle: formatPublishedAt(item.publishedAt),
          href: item.href,
          downloadHref: item.downloadHref,
        })),
    },
  };
}
```

- [ ] **Step 2: Update investor-materials/index.tsx**

Replace `getTranslations` + `getLocale` with static:

```tsx
import clsx from "clsx";
import AppContent from "@/shared/ui/app-content";
import InvestorMaterialsHeading from "@/app/sections/investor-materials/components/investor-materials-heading";
import InvestorMaterialsDocumentGrid from "@/app/sections/investor-materials/components/investor-materials-document-grid";
import { buildInvestorDocumentGroups } from "@/app/sections/investor-materials/data";
import { getInvestorServedDocuments } from "@/app/sections/investor-materials/server/documents";
import { appSectionIds } from "@/shared/config/app";

export default async function InvestorMaterialsSection() {
  const documents = await getInvestorServedDocuments();
  const investorDocumentGroups = buildInvestorDocumentGroups(documents);

  return (
    <section id={appSectionIds.investorMaterials} className={clsx("bg-app-neutral-50 py-10 xl:py-16")}>
      <AppContent className="flex flex-col gap-6 xl:gap-13 px-4 md:px-6 xl:px-0">
        <InvestorMaterialsHeading />
        <InvestorMaterialsDocumentGrid investorDocumentGroups={investorDocumentGroups} />
      </AppContent>
    </section>
  );
}
```

- [ ] **Step 3: Update investor-materials-heading/index.tsx**

Replace `useTranslations("HomePage.investorMaterials")` with static text:

- `t("heading.title")` → `"Tài liệu nhà đầu tư"`
- `t("heading.description")` → `"Cập nhật thông tin mới nhất về tình hình hoạt động và kết quả kinh doanh của F88"`

- [ ] **Step 4: Update investor-materials-document-grid/index.tsx**

Read the file first to see specific translation keys, then:

- Remove `useTranslations("HomePage.investorMaterials")`
- Replace `t("filters.businessResults")` → `"Kết quả kinh doanh"`
- Replace `t("filters.ipoMaterials")` → `"Tài liệu IPO"`
- (And other keys used in this file)

- [ ] **Step 5: Commit**

Run: `git add -A && git commit -m "refactor: replace investor-materials translations with static Vietnamese text"`

---

### Task 6: Replace translations in banner and offering-info sections

**Files:**

- Modify: `apps/web/src/app/sections/banner/components/banner-badge/index.tsx`
- Modify: `apps/web/src/app/sections/banner/components/banner-heading/index.tsx`
- Modify: `apps/web/src/app/sections/banner/components/banner-content/index.tsx`
- Modify: `apps/web/src/app/sections/banner/components/banner-actions/index.tsx`
- Modify: `apps/web/src/app/sections/offering-info/components/offering-info-heading/index.tsx`
- Modify: `apps/web/src/app/sections/offering-info/components/offering-info-stats/index.tsx`

For each file:

1. Remove `import { useTranslations } from "next-intl"`
2. Remove `const t = useTranslations("...")`
3. Replace `{t("key")}` with Vietnamese text inferred from context

Read each file to determine exact translation keys and replace with appropriate Vietnamese.

- [ ] **Step 1-6: Update each file individually**

Each step: read the file, make the edit, verify.

- [ ] **Step 7: Commit**

Run: `git add -A && git commit -m "refactor: replace banner and offering-info translations with static text"`

---

### Task 7: Replace translations in F88 overview, journey, and ipo-roadmap sections

**Files:**

- Modify: `apps/web/src/app/sections/f88-overview/index.tsx`
- Modify: `apps/web/src/app/sections/f88-overview/components/text-description/index.tsx`
- Modify: `apps/web/src/app/sections/f88-overview/components/image-description/index.tsx`
- Modify: `apps/web/src/app/sections/f88-journey/components/journey-heading/index.tsx`
- Modify: `apps/web/src/app/sections/f88-journey/components/journey-timeline/index.tsx`
- Modify: `apps/web/src/app/sections/ipo-roadmap/components/ipo-roadmap-heading/index.tsx`
- Modify: `apps/web/src/app/sections/ipo-roadmap/components/ipo-roadmap-timeline/index.tsx`
- Modify: `apps/web/src/app/sections/ipo-roadmap/components/ipo-roadmap-timeline/components/ipo-roadmap-timeline-content/index.tsx`
- Modify: `apps/web/src/app/sections/ipo-roadmap/components/ipo-roadmap-cta/index.tsx`

For each file, same pattern: remove next-intl imports, replace `t("key")` with static Vietnamese.

- [ ] **Step 1-9: Update each file individually**
- [ ] **Step 10: Commit**

---

### Task 8: Replace translations in market-leader, strategic-direction, breakthrough-growth sections

**Files:**

- Modify: `apps/web/src/app/sections/f88-market-leader/components/market-leader-heading/index.tsx`
- Modify: `apps/web/src/app/sections/f88-market-leader/components/market-leader-highlights/index.tsx`
- Modify: `apps/web/src/app/sections/f88-market-leader/components/market-leader-details/index.tsx`
- Modify: `apps/web/src/app/sections/f88-market-leader/components/market-leader-ecosystem/index.tsx`
- Modify: `apps/web/src/app/sections/f88-strategic-direction/components/strategic-direction-heading/index.tsx`
- Modify: `apps/web/src/app/sections/f88-strategic-direction/components/strategic-direction-values/index.tsx`
- Modify: `apps/web/src/app/sections/f88-strategic-direction/components/strategic-direction-pillars/index.tsx`
- Modify: `apps/web/src/app/sections/f88-breakthrough-growth/index.tsx`
- Modify: `apps/web/src/app/sections/f88-breakthrough-growth/components/breakthrough-growth-heading/index.tsx`
- Modify: `apps/web/src/app/sections/f88-breakthrough-growth/components/breakthrough-growth-table/index.tsx`
- Modify: `apps/web/src/app/sections/f88-breakthrough-growth/components/breakthrough-growth-summary-cards/index.tsx`
- Modify: `apps/web/src/app/sections/f88-breakthrough-growth/components/breakthrough-growth-chart-card/index.tsx`

For each file, same pattern.

- [ ] **Step 1-12: Update each file individually**
- [ ] **Step 13: Commit**

---

### Task 9: Replace translations in investor-faq, news-section, purchase-guide, header-menu, footer

**Files:**

- Modify: `apps/web/src/app/sections/investor-faq/components/investor-faq-heading/index.tsx`
- Modify: `apps/web/src/app/sections/investor-faq/components/investor-faq-accordion/index.tsx`
- Modify: `apps/web/src/app/sections/news-section/components/news-section-heading/index.tsx`
- Modify: `apps/web/src/app/sections/news-section/components/news-section-grid/index.tsx`
- Modify: `apps/web/src/app/sections/news-section/components/news-section-card/index.tsx`
- Modify: `apps/web/src/app/sections/news-section/components/news-section-more-link/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-heading/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-steps/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-contact-cards/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-guide-modal/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-signup-panel/index.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-signup-panel/purchase-guide-signup-form.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-signup-panel/recaptcha-confirm-modal.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/components/purchase-guide-signup-panel/signup-form-success-modal.tsx`
- Modify: `apps/web/src/app/sections/purchase-guide/hooks/usePurchaseGuideSignupForm/index.ts`
- Modify: `apps/web/src/shared/ui/app-header/components/header-menu/index.tsx`
- Modify: `apps/web/src/shared/ui/app-footer/index.tsx`

For each file, same pattern.

- [ ] **Step 1-18: Update each file individually**
- [ ] **Step 19: Commit**

---

### Task 10: Verify build

- [ ] **Step 1: Run type check**

Run: `npx tsc --noEmit` from `apps/web/`

Expected: No TypeScript errors.

- [ ] **Step 2: Run build**

Run: `npm run build` (or equivalent build command)

Expected: Build succeeds.

- [ ] **Step 3: Final commit**

`git add -A && git commit -m "chore: complete i18n removal — build verified"`
